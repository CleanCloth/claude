# Billy API access token for CleanCloth Rengøring.
# Generate under Settings → Access tokens in billyapp.com. Tokens are permanent
# and scoped to a single organization. Do not commit the real value.
BILLY_TOKEN=
